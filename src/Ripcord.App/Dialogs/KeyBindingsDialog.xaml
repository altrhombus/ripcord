<?xml version="1.0" encoding="utf-8" ?>
<ContentDialog
    x:Class="Ripcord_App.Dialogs.KeyBindingsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Ripcord_App.Dialogs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Keyboard controls"
    CloseButtonText="Done"
    DefaultButton="Primary"
    PrimaryButtonText="Reset to defaults"
    mc:Ignorable="d">

    <!--
        Rebinding works by capture rather than by picking from a key list: the user presses the key they want,
        which is both faster and the only way to bind keys a dropdown would not think to offer.
    -->
    <Grid RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <InfoBar
            x:Name="CaptureBar"
            Grid.Row="0"
            IsClosable="False"
            IsOpen="False"
            Severity="Informational"
            Title="Press a key" />

        <TextBlock
            Grid.Row="1"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Style="{StaticResource CaptionTextBlockStyle}"
            Text="Escape, F3 and F11 stay with the app (leave the stream, diagnostics, fullscreen) and cannot be bound. Sticks move at full deflection while a key is held."
            TextWrapping="Wrap" />

        <ScrollViewer Grid.Row="2" MaxHeight="420">
            <ItemsControl x:Name="BindingList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="local:KeyBindingRow">
                        <Grid Padding="0,4" ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock
                                VerticalAlignment="Center"
                                Text="{x:Bind ActionName}"
                                TextWrapping="Wrap" />
                            <TextBlock
                                Grid.Column="1"
                                MinWidth="120"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{x:Bind KeyNames}"
                                TextAlignment="Right" />
                            <Button
                                Grid.Column="2"
                                Click="Rebind_Click"
                                Content="Change"
                                Tag="{x:Bind Action}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</ContentDialog>
